<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.ensicaen.present.present">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
                <activity
                    android:name=".splash.SplashScreenActivity"
                    android:theme="@style/MyTheme.Splash">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN" />
                        <category android:name="android.intent.category.LAUNCHER" />
                    </intent-filter>
                </activity>
        -->


        <activity
            android:name=".login.LoginActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_login" />

        <!--        <activity android:name=".dashboard.DashboardActivity">
                </activity>-->
        <activity android:name=".dashboard.DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".configure_call.ConfigureCallActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".generate_code.GenerateCodeActivity"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".select_groups.SelectGroups"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".enter_code.EnterCodeActivity"
            android:label="@string/app_name">

        </activity>
    </application>

</manifest>